export default function App() {
  return (
    <div className="min-h-screen bg-canvas text-ink font-mono">
      <header className="border-b border-border px-6 py-4">
        <h1 className="text-lg">
          <span className="text-accent">chainscope</span>
          <span className="text-muted"> // ml supply-chain transparency</span>
        </h1>
      </header>
      <main className="p-6">
        <p className="text-muted">
          Scaffold running. Endpoints, models, and UI land in later commits —
          see COMMIT_PLAN.md.
        </p>
      </main>
    </div>
  );
}
