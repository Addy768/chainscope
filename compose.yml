services:
  backend:
    build: ./backend
    ports: ["5000:5000"]
    environment:
      CORS_ORIGINS: "http://localhost:5173"
    volumes:
      - ./ml/artifacts:/app/../ml/artifacts:ro

  frontend:
    build: ./frontend
    ports: ["5173:80"]
    depends_on: [backend]
