# Media

Screenshots and demo GIF land here in the "docs: add screenshots" commit
(Phase 8). Drop them into this folder and update paths in the root
`README.md`.

Suggested names:
- `demo.gif` — 6-second upload → classify → detect → risk score flow
- `metrics.png` — screenshot of `/metrics` page
- `dashboard.png` — screenshot of home dashboard
