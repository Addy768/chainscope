# Docs — ChainScope

- `DATA_CARD.md` — data sources, licenses, biases (Phase 2)
- `EVALUATION.md` — test-set metrics for every model (Phase 8)
- `DEPLOYMENT.md` — Cloud Run + Vercel walkthrough (Phase 8)
- `model_cards/` — one per trained model
- `figures/` — plots exported from notebooks (SHAP, confusion matrix, PR curves)
- `media/` — screenshots + demo GIF used in the root README
